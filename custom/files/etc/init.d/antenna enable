#!/bin/sh /etc/rc.common
# /etc/init.d/antenna
# Восстанавливает сохранённый режим антенны при старте системы.

START=95
STOP=10

start() {
    MODE=$(cat /etc/antenna_mode 2>/dev/null)
    [ -n "$MODE" ] || MODE=internal
    /usr/bin/antenna-switch.sh "$MODE"
}
